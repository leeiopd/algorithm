import sys
sys.stdin = open("04_input.txt")

T = int(input())

def choice(map, n, cnt):

    if len(nums) == N:
        result.apend(nums)
        return

    else:
        for i in range(n-1, -1, -1):





for case in range(1, T+1):
    N = int(input())
    maps = []
    for n in range(N):
        maps.append(list(map(int, input().split())))

    cnt = -1
    nums = []
    result = []